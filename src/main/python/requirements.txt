# Frontend Python Local Backend Dependencies

# Vector storage and search
faiss-cpu>=1.7.4
aiosqlite>=0.19.0

# HTTP client for backend communication (for embeddings)
aiohttp>=3.9.0

# Additional utilities
numpy>=1.24.0,<2.0

# User data directories
platformdirs>=3.0.0

# Wake-word detection
openwakeword

# Computer control (mouse, keyboard, screenshot)
pyautogui>=0.9.54
pynput>=1.7.6

# System information
psutil>=5.9.0

# Clipboard access
pyperclip>=1.8.2

# Image processing (for screenshots)
Pillow>=10.0.0

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Testing
pytest>=8.1.1
pytest-asyncio>=0.23.6

# Browser automation
playwright>=1.40.0
# Browser Use is vendored in-repo at frontend/src/main/python/tools/browser/browser_use
httpx>=0.28.1
bubus>=1.5.6
cdp-use>=1.4.4
google-genai>=1.50.0,<2.0.0
openai>=2.7.2,<3.0.0
pyotp>=2.9.0

# Gitignore parsing
pathspec>=0.10.0

# Platform-specific window management
# Windows
pywin32>=306; sys_platform == 'win32'

# macOS (AppKit is part of pyobjc, but we can use it directly if available)
# Note: AppKit is typically available via system Python on macOS
# If not available, install: pip install pyobjc-framework-AppKit

# Linux: xdotool should be installed via system package manager
# sudo apt-get install xdotool  (Debian/Ubuntu)
# sudo yum install xdotool      (RHEL/CentOS)
