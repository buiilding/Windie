/**
 * Provides the linux module for the Electron main process.
 */

module.exports = function enableContentProtectionSafely() {
  // Linux uses chat-pill hide/restore for clean screenshots instead of Electron content protection.
};
