/**
 * WindieOS persisted renderer storage keys for the chat desktop UI skin.
 */

export const RENDERER_STORAGE_KEYS = Object.freeze({
  config: 'windieos-config',
  memoryRetrievalInjection: 'windieos-memory-retrieval-injection-enabled',
  permissionOnboarding: 'windieos-permission-onboarding',
});
