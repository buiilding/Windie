/**
 * WindieOS default model selection for renderer config initialization.
 */

export const DEFAULT_MODEL_SELECTION = Object.freeze({
  mode: 'online',
  provider: 'openai',
  modelId: 'gpt-5.4@@gpt-5-4-none-thinking',
});
