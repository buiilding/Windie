{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "root": {
      "type": "string",
      "description": "Absolute or relative repository path to inspect."
    },
    "max_files": {
      "type": "integer",
      "description": "Maximum number of files to include in the snapshot.",
      "minimum": 1,
      "maximum": 50,
      "default": 20
    }
  },
  "required": ["root"]
}
